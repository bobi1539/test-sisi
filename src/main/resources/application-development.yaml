server:
  port: 8080
  host: localhost:8080
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: bobirival
    password: password@147
    url: jdbc:postgresql://localhost:5432/test-sisi
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect